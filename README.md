# Plane

**Plane** is a personal spatial-design sandbox: a 2D top-down floor-plan editor for recreating apartments and trying furniture layouts.

## Run

```bash
npm install
npm run dev
```

Open [http://localhost:5173](http://localhost:5173).

Projects persist in the browser via localStorage. Units are inches internally and feet/inches in the UI.
